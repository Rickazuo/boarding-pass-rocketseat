import PlanningPassInfo from "../../components/PlanningPassInfo";
import styles from "./styles.module.css";

const Pass = () => {
    return (
        <div>
            <PlanningPassInfo />
        </div>
    );
};

export default Pass;
